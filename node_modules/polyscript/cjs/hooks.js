'use strict';
const beforeRun = 'BeforeRun';
const afterRun = 'AfterRun';

const worker = [
    'onReady',
    `code${beforeRun}`,
    `code${beforeRun}Async`,
    `code${afterRun}`,
    `code${afterRun}Async`,
];
exports.worker = worker;

const main = [
    ...worker,
    `on${beforeRun}`,
    `on${beforeRun}Async`,
    `on${afterRun}`,
    `on${afterRun}Async`,
];
exports.main = main;
